---
title: "Analysis part 1"
author: "Ndiaga FALL"
date: "25/07/2018"
output:
  html_document: default
  pdf_document: default
  md_document: default
---


Le package datasets contient plusieurs dataset

```{r}
data(package="datasets")
data(trees)
```


Nous allons nous intéresser au dataset "Trees"

```{r echo=FALSE}
head(trees)
tail(trees)
colnames(trees)
```
On a un dataset qui explique l'évolution d'un arbre
Voici les 3 variables que nous allons étudier : la circonférence, la hauteur et le volume de l'arbre.

On va ainsi déterminer la structure du dataset

```{r}
str(trees)
summary(trees)
```

## Exploration du jeu de données

On peut voir qu'on a un dataset de 31 lignes et 3 colonnes
les variables Girth, Height et Volume sont des variables quantitatives

On a déterminés les stats descriptives des 3 variables à travers
la médiane, la moyenne, le minimum et le maximum , le 1er quartile et le 3e quartile.

```{r echo=TRUE}
dim(trees)
```

## nuage de points


```{r echo=TRUE}
plot(trees, col = 1:5)
```

On peut voir le nuage de points des différentes variables ainsi que leur évolution.

Essayons de voir la correlation entre les différentes variables

```{r}
cor(trees)
```

```{r echo=FALSE}
hist(trees$Height, col = "green", main = "distribution de la hauteur")
hist(trees$Girth, col = "green", main = "distribution de la circonférence")
hist(trees$Volume, col = "green", main = "distribution du volume")
```

Voici la distribution de la hauteur de l'arbre.

Visualisation du dataset

```{r echo=TRUE}
library(ggplot2)
ggplot(data = trees) +
  geom_point(aes(x = trees$Volume, y = trees$Height), colour = "yellow") +
  geom_line(aes(x = trees$Volume, y = trees$Height), colour = "blue") +
  ggtitle("Height vs Volume") +
  xlab("Volume") +
  ylab("Height")
```


Boites à moustaches des stats descriptives pour les 3 variables

```{r echo=TRUE}
boxplot(trees, col = 1:3)
```


```{r}
library(ggplot2)
ggplot(data = trees) +
  geom_point(aes(x = trees$Girth, y = trees$Height), colour = "red") +
  geom_line(aes(x = trees$Girth, y = trees$Height), colour = "green") +
  ggtitle("Height vs Girth") +
  xlab("Girth") +
  ylab("Height")
```


